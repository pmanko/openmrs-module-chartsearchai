<?xml version="1.0" encoding="UTF-8"?>

<module configVersion="1.2">

	<!-- Module Properties -->
	<id>${project.parent.artifactId}</id>
	<name>${project.parent.name}</name>
	<version>${project.parent.version}</version>
	<package>org.openmrs.module.chartsearchai</package>
	<author>OpenMRS</author>
	<description>${project.parent.description}</description>
	<updateURL>https://modules.openmrs.org/modules/download/${project.parent.artifactId}/update.rdf</updateURL>
	<require_version>${openmrsPlatformVersion}</require_version>

	<require_modules>
		<require_module version="2.44.0">org.openmrs.module.webservices.rest</require_module>
	</require_modules>

	<!-- Module Activator -->
	<activator>org.openmrs.module.chartsearchai.ChartSearchAiModuleActivator</activator>

	<!-- Hibernate Mappings -->
	<mappingFiles>
		ChartEmbedding.hbm.xml
		ChartSearchAuditLog.hbm.xml
	</mappingFiles>

	<!-- Privileges -->
	<privilege>
		<name>AI Query Patient Data</name>
		<description>Allows querying patient charts using the AI-powered chart search</description>
	</privilege>

	<privilege>
		<name>View AI Audit Logs</name>
		<description>Allows viewing the audit log of AI chart search queries</description>
	</privilege>

	<!-- Global Properties -->
	<globalProperty>
		<property>chartsearchai.llm.modelFilePath</property>
		<defaultValue></defaultValue>
		<description>Relative path, including the filename, within the OpenMRS application data directory to the GGUF LLM model file used for inference. Required for both search modes. Example: chartsearchai/Llama-3.2-3B-Instruct-Q4_K_M.gguf (resolves to &lt;application-data-directory&gt;/chartsearchai/Llama-3.2-3B-Instruct-Q4_K_M.gguf). The recommended model is Llama 3.2 3B Instruct (Q4_K_M quantization, ~2GB). Download from https://huggingface.co/bartowski/Llama-3.2-3B-Instruct-GGUF. Path traversal (e.g. "..") is not allowed.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.embedding.preFilter</property>
		<defaultValue>true</defaultValue>
		<description>When true, uses embedding similarity to pre-filter patient records to the most relevant ones before sending to the LLM. Embeddings are indexed on first patient access and kept up to date automatically. Set to false to send the full patient chart to the LLM without any embedding-based filtering. Default: true.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.llm.chatTemplate</property>
		<defaultValue>llama3</defaultValue>
		<description>Chat template for formatting prompts sent to the LLM. Use a preset name (llama3, mistral, phi3, chatml, gemma) or a custom template string with {system} and {user} placeholders. Default: llama3.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.llm.systemPrompt</property>
		<defaultValue>You are a clinical assistant helping a clinician review a patient's chart. Answer ONLY the specific question asked. Use only the patient records below (sorted most recent first). Include the relevant details from the records in your answer and cite EVERY record you use by its number in brackets (e.g. [1], [3]). Do not list records that are irrelevant to the question. If the records do not contain enough information to answer, respond with: "No relevant information was found in the patient's records." Keep your answer concise — one to three sentences. Respond with ONLY a JSON object in this exact format: {"answer": "your answer here"}

Examples:

Records:
[1] Condition: Zorblitis
[2] Medication: Xanthuril 50mg daily
[3] Flobnar level: 12.4
[4] Flobnar level: 9.8

Question: What are the Flobnar levels?
{"answer": "The Flobnar levels are 12.4 [3] and 9.8 [4]."}

Question: Does the patient have diabetes?
{"answer": "No relevant information was found in the patient's records."}</defaultValue>
		<description>System prompt sent to the LLM before patient records and the question. Customize this to adjust the LLM's behavior, tone, or citation format.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.llm.timeoutSeconds</property>
		<defaultValue>120</defaultValue>
		<description>Maximum number of seconds to wait for the LLM to generate a response before timing out.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.rateLimitPerMinute</property>
		<defaultValue>10</defaultValue>
		<description>Maximum number of AI chart search queries a single user can make per minute. Set to 0 to disable rate limiting.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.auditLogRetentionDays</property>
		<defaultValue>90</defaultValue>
		<description>Number of days to retain AI chart search audit log entries. Entries older than this are automatically purged by a daily scheduled task. Set to 0 to disable purging and retain all logs indefinitely.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.cacheTtlMinutes</property>
		<defaultValue>0</defaultValue>
		<description>Number of minutes to cache AI chart search responses for identical patient and question combinations. Avoids redundant LLM calls. Set to 0 to disable caching (default).</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.embedding.provider</property>
		<defaultValue>term-frequency</defaultValue>
		<description>Embedding provider for the embedding search mode: "term-frequency" (default hash-based vectors) or "onnx" (semantic vectors via all-MiniLM-L6-v2).</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.embedding.modelFilePath</property>
		<defaultValue></defaultValue>
		<description>Relative path, including the filename, within the OpenMRS application data directory to the ONNX embedding model file. Required only when chartsearchai.embedding.provider is set to "onnx". Example: chartsearchai/all-MiniLM-L6-v2.onnx (resolves to &lt;application-data-directory&gt;/chartsearchai/all-MiniLM-L6-v2.onnx). Download the model.onnx file from https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2 and rename it. Path traversal (e.g. "..") is not allowed.</description>
	</globalProperty>

	<globalProperty>
		<property>chartsearchai.embedding.vocabFilePath</property>
		<defaultValue></defaultValue>
		<description>Relative path, including the filename, within the OpenMRS application data directory to the WordPiece vocabulary file (vocab.txt) for the ONNX embedding model. Required only when chartsearchai.embedding.provider is set to "onnx". Example: chartsearchai/vocab.txt (resolves to &lt;application-data-directory&gt;/chartsearchai/vocab.txt). Download vocab.txt from the same model repository as the ONNX model (e.g. https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2). Path traversal (e.g. "..") is not allowed.</description>
	</globalProperty>

	<!-- AOP -->
	<advice>
		<point>org.openmrs.api.EncounterService</point>
		<class>org.openmrs.module.chartsearchai.api.EncounterIndexingAdvice</class>
	</advice>

	<advice>
		<point>org.openmrs.api.ObsService</point>
		<class>org.openmrs.module.chartsearchai.api.ObsIndexingAdvice</class>
	</advice>

	<advice>
		<point>org.openmrs.api.ConditionService</point>
		<class>org.openmrs.module.chartsearchai.api.PatientDataIndexingAdvice</class>
	</advice>

	<advice>
		<point>org.openmrs.api.PatientService</point>
		<class>org.openmrs.module.chartsearchai.api.PatientDataIndexingAdvice</class>
	</advice>

	<advice>
		<point>org.openmrs.api.OrderService</point>
		<class>org.openmrs.module.chartsearchai.api.PatientDataIndexingAdvice</class>
	</advice>

	<!-- Internationalization -->
	<messages>
		<lang>en</lang>
		<file>messages.properties</file>
	</messages>

</module>
